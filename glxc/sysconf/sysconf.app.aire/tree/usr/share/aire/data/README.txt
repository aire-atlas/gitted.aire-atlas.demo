This directory is good to hold common data files that not meant
to change often.

It's better to put Shapefiles, Rasters and other files here rather than in
the AIRE instance directory in /var/lib/aire/<instance>/app/data, which
is specific to each AIRE instance.
